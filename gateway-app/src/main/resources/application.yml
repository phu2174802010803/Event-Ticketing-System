spring:
  application:
    name: gateway-app
  data:
    redis:
      host: localhost
      port: 6379
  cloud:
    gateway:
      routes:
        - id: identity-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/register, /api/auth/**, /api/users/**
        - id: event-service
          uri: http://localhost:8083
          predicates:
            - Path=/api/events/public/**, /api/events/**, /ws/**, /ws-native/**
          filters:
            - name: ModifyResponseBody
              args:
                  inClass: java.lang.String
                  outClass: java.lang.String
                  rewriteFunction: "#{@cacheRewriteFunction}"
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10   # Tăng lên 10 yêu cầu/phút
                redis-rate-limiter.burstCapacity: 20    # Tăng lên 20 yêu cầu đột biến
                key-resolver: "#{@remoteAddrKeyResolver}"
        - id: ticket-service
          uri: http://localhost:8084
          predicates:
            - Path=/api/tickets/**, /api/queue/**
      default-filters:
        - AuthFilter
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:5173"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
            allowedHeaders:
              - "*"
            allowCredentials: true
            maxAge: 3600
server:
  port: 8080
  http2:
    enabled: true                               # Bật HTTP/2
identity:
  service:
    url: http://localhost:8081
logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    com.example.gatewayapp: DEBUG